<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BlueApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->

<!--         <item name="colorPrimary">@color/white</item>-->
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.BlueApp" parent="Base.Theme.BlueApp" />

    <style name="rubik_medium_black_12" parent="android:Widget.TextView">
        <!-- Customize TextView attributes here -->
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">12sp</item>
        <item name="fontFamily">@font/rubik_semibold</item>
        <item name="lineSpacing">14dp</item>
        <!-- Add more attributes as needed -->
    </style>

    <style name="rubik_medium_grey_12" parent="android:Widget.TextView">
        <!-- Customize TextView attributes here -->
        <item name="android:textColor">@color/grey</item>
        <item name="android:textSize">12sp</item>
        <item name="fontFamily">@font/rubik_semibold</item>
        <item name="lineSpacing">14dp</item>
        <!-- Add more attributes as needed -->
    </style>

    <style name="rubik_medium_darkgreen_12" parent="android:Widget.TextView">
        <!-- Customize TextView attributes here -->
        <item name="android:textColor">@color/darkgreen</item>
        <item name="android:textSize">12sp</item>
        <item name="fontFamily">@font/rubik_semibold</item>
        <item name="lineSpacing">14dp</item>
        <!-- Add more attributes as needed -->
    </style>

    <style name="NoBackgroundDialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowBackground">@null</item>
    </style>

</resources>